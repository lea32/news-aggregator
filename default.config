webserver {
    host = 'localhost'
    port = 8080
}

database {
    url = 'jdbc:mysql://localhost/aggregator'
    username = 'javauser'
    password = 'javauser'

    pool {
        minSize = 5
        maxSize = 20
        maxIdle = 30 * minute
        maxExcessIdle = 3 * minute
        maxAge = 60 * minute
    }
}

